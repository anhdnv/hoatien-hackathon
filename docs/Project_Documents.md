# IT Technical Support Knowledge Base

## 📞 Thông tin liên hệ IT Support

**Hotline hỗ trợ 24/7:** 1900-1234 (nhánh 5)  
**Email hỗ trợ:** itsupport@company.com  
**Email khẩn cấp:** it.emergency@company.com  
**Địa chỉ văn phòng IT:** Tầng 3, Tòa nhà A, Số 123 Đường ABC, Quận 1, TP.HCM  
**Giờ làm việc:** Thứ 2 - Thứ 6: 8:00 - 17:30 | Thứ 7: 8:00 - 12:00  
**Hệ thống ticket:** https://helpdesk.company.com  
**Portal tự phục vụ:** https://selfservice.company.com  
**VPN Gateway:** vpn.company.com  
**Remote Desktop Gateway:** rdp.company.com

**Liên hệ theo chuyên môn:**

- Hỗ trợ phần cứng: hardware@company.com - Ext: 301
- Hỗ trợ phần mềm: software@company.com - Ext: 302
- Hỗ trợ mạng: network@company.com - Ext: 303
- Bảo mật: security@company.com - Ext: 304
- Quản lý tài khoản: account@company.com - Ext: 305

**Thời gian phản hồi:**

- Ưu tiên Khẩn cấp (P1): Trong vòng 30 phút
- Ưu tiên Cao (P2): Trong vòng 2 giờ
- Ưu tiên Trung bình (P3): Trong vòng 8 giờ
- Ưu tiên Thấp (P4): Trong vòng 24 giờ

---

## 📋 Danh sách 30 vấn đề kỹ thuật thường gặp (Q&A)

---

**Q1. Làm sao để reset mật khẩu Windows?**  
A1: Để reset mật khẩu Windows, bạn hãy thực hiện các bước sau:

1. Truy cập trang reset mật khẩu nội bộ hoặc nhấn tổ hợp phím Ctrl+Alt+Del và chọn 'Change Password'.
2. Nhập username của bạn và làm theo hướng dẫn trên màn hình.
3. Nếu hệ thống yêu cầu xác thực qua email hoặc điện thoại, hãy xác nhận mã OTP được gửi về.
4. Nếu không thể tự reset, liên hệ bộ phận IT qua ticket hoặc hotline để được hỗ trợ trực tiếp. Lưu ý: Không chia sẻ mật khẩu mới với bất kỳ ai.

**Q2. Máy tính không kết nối được WiFi, xử lý thế nào?**  
A2: Hãy kiểm tra lần lượt các bước sau:

1. Đảm bảo công tắc WiFi trên laptop đã bật.
2. Kiểm tra biểu tượng WiFi trên thanh taskbar, nếu có dấu X hoặc chấm than, thử tắt/bật lại WiFi.
3. Khởi động lại máy tính.
4. Kiểm tra router/modem, thử kết nối thiết bị khác để xác định lỗi từ máy hay mạng.
5. Nếu vẫn không được, liên hệ IT để kiểm tra driver hoặc cấu hình mạng.

**Q3. Không truy cập được email công ty?**  
A3: Thực hiện các bước sau:

1. Kiểm tra kết nối Internet.
2. Đăng nhập lại email, đảm bảo nhập đúng domain và mật khẩu.
3. Xóa cache/trình duyệt hoặc thử đăng nhập trên trình duyệt khác.
4. Nếu quên mật khẩu, sử dụng chức năng 'Quên mật khẩu' hoặc liên hệ IT để reset.
5. Nếu nhận được thông báo lỗi, chụp màn hình và gửi kèm khi liên hệ IT.

**Q4. Làm sao để cài đặt phần mềm mới?**  
A4: Quy trình chuẩn như sau:

1. Gửi yêu cầu cài đặt phần mềm qua hệ thống ticket, ghi rõ tên phần mềm và mục đích sử dụng.
2. IT sẽ kiểm tra tính hợp lệ (bản quyền, chính sách công ty).
3. Nếu được phê duyệt, IT sẽ cài đặt trực tiếp hoặc gửi hướng dẫn tự cài đặt.
4. Không tự ý cài phần mềm không rõ nguồn gốc để tránh rủi ro bảo mật.

**Q5. Máy in không nhận lệnh in?**  
A5: Xử lý theo các bước:

1. Kiểm tra kết nối vật lý giữa máy tính và máy in (cáp USB hoặc mạng LAN).
2. Đảm bảo máy in đã bật nguồn và không báo lỗi giấy/kẹt giấy.
3. Kiểm tra driver máy in trên máy tính, cài lại nếu cần.
4. Thử in từ ứng dụng khác hoặc file khác để loại trừ lỗi phần mềm.
5. Nếu vẫn không được, liên hệ IT để kiểm tra sâu hơn hoặc thay thế thiết bị.

**Q6. Không truy cập được VPN?**  
A6: Thực hiện các bước sau:

1. Kiểm tra username/password, đảm bảo nhập đúng.
2. Kiểm tra kết nối Internet ổn định.
3. Thử kết nối lại sau vài phút, có thể hệ thống đang bảo trì.
4. Nếu nhận được thông báo lỗi, ghi lại mã lỗi và gửi cho IT.
5. IT sẽ kiểm tra tài khoản, cấu hình VPN và hỗ trợ khắc phục.

**Q7. Làm sao để báo cáo bug phần mềm?**  
A7: Quy trình chuẩn:

1. Đăng nhập hệ thống Jira hoặc công cụ quản lý bug của công ty.
2. Tạo ticket mới, ghi rõ các bước tái hiện lỗi, kết quả mong đợi và kết quả thực tế.
3. Đính kèm hình ảnh/video minh họa nếu có.
4. Chọn đúng mức độ ưu tiên và phân loại lỗi.
5. Theo dõi tiến trình xử lý và phản hồi từ bộ phận phát triển.

**Q8. Không mở được file Excel?**  
A8: Xử lý như sau:

1. Kiểm tra phiên bản Office, cập nhật nếu cần.
2. Thử mở file trên máy khác để xác định lỗi do file hay phần mềm.
3. Kiểm tra quyền truy cập file nếu file nằm trên mạng nội bộ.
4. Nếu file bị lỗi, thử dùng chức năng 'Open and Repair' của Excel.
5. Nếu vẫn không được, gửi file cho IT kiểm tra và phục hồi.

**Q9. Máy tính chạy chậm bất thường?**  
A9: Các bước kiểm tra:

1. Kiểm tra các phần mềm chạy nền, tắt ứng dụng không cần thiết.
2. Quét virus/malware bằng phần mềm diệt virus bản quyền.
3. Kiểm tra dung lượng ổ cứng, xóa file rác nếu đầy.
4. Khởi động lại máy tính.
5. Nếu vẫn chậm, liên hệ IT để kiểm tra phần cứng (RAM, ổ cứng).

**Q10. Không truy cập được hệ thống quản lý nhân sự?**  
A10: Thực hiện các bước:

1. Kiểm tra kết nối mạng.
2. Đảm bảo bạn có quyền truy cập hệ thống, liên hệ IT để xác nhận.
3. Thử đăng nhập trên trình duyệt khác hoặc xóa cache.
4. Nếu nhận được thông báo lỗi, gửi chi tiết cho IT để kiểm tra.

**Q11. Làm sao để đổi mật khẩu email?**  
A11: Hướng dẫn:

1. Đăng nhập vào trang quản lý email.
2. Chọn mục 'Đổi mật khẩu', nhập mật khẩu cũ và mật khẩu mới.
3. Xác nhận thay đổi, kiểm tra email xác thực nếu có.
4. Không sử dụng mật khẩu dễ đoán, nên kết hợp chữ hoa, chữ thường, số và ký tự đặc biệt.

**Q12. Không nhận được email từ đối tác?**  
A12: Kiểm tra như sau:

1. Kiểm tra thư mục spam/junk.
2. Kiểm tra bộ lọc email, whitelist domain đối tác nếu cần.
3. Đảm bảo đối tác gửi đúng địa chỉ email.
4. Liên hệ IT để kiểm tra hệ thống mail server nếu nghi ngờ bị chặn.

**Q13. Làm sao để truy cập hệ thống từ xa?**  
A13: Quy trình chuẩn:

1. Sử dụng VPN của công ty, đăng nhập bằng tài khoản nội bộ.
2. Đảm bảo thiết bị cá nhân tuân thủ chính sách bảo mật (cài phần mềm diệt virus, không cài phần mềm lạ).
3. Sau khi kết nối VPN, truy cập hệ thống như bình thường.
4. Nếu gặp lỗi, liên hệ IT để kiểm tra cấu hình hoặc cấp lại quyền.

**Q14. Không cài được phần mềm do lỗi bản quyền?**  
A14: Xử lý như sau:

1. Chỉ sử dụng phần mềm có bản quyền, không cài phần mềm crack.
2. Gửi yêu cầu cấp license cho IT nếu cần sử dụng phần mềm mới.
3. IT sẽ kiểm tra và cấp license hợp lệ hoặc hướng dẫn sử dụng phần mềm thay thế.

**Q15. Máy tính không nhận USB?**  
A15: Kiểm tra các bước:

1. Thử cắm USB sang cổng khác trên máy tính.
2. Kiểm tra driver USB, cập nhật nếu cần.
3. Thử USB trên máy khác để xác định lỗi do thiết bị hay máy tính.
4. Nếu USB chứa dữ liệu quan trọng, liên hệ IT để kiểm tra và phục hồi dữ liệu.

**Q16. Không truy cập được website nội bộ?**  
A16: Thực hiện các bước:

1. Kiểm tra kết nối mạng nội bộ hoặc VPN.
2. Kiểm tra quyền truy cập website, liên hệ IT để xác nhận.
3. Thử truy cập trên trình duyệt khác hoặc xóa cache.
4. Nếu website bị lỗi, gửi thông báo cho IT kèm ảnh chụp màn hình.

**Q17. Làm sao để backup dữ liệu cá nhân?**  
A17: Hướng dẫn:

1. Sử dụng dịch vụ lưu trữ đám mây như OneDrive, Google Drive hoặc ổ cứng ngoài.
2. Đặt lịch backup định kỳ, kiểm tra dữ liệu sau khi backup.
3. Đảm bảo dữ liệu backup tuân thủ chính sách bảo mật của công ty.
4. Không lưu dữ liệu nhạy cảm trên thiết bị cá nhân không bảo mật.

**Q18. Không đăng nhập được hệ thống quản lý dự án?**  
A18: Kiểm tra như sau:

1. Kiểm tra username/password, thử reset mật khẩu nếu quên.
2. Kiểm tra quyền truy cập, liên hệ IT để cấp lại quyền nếu cần.
3. Thử đăng nhập trên trình duyệt khác hoặc xóa cache.
4. Nếu nhận được thông báo lỗi, gửi chi tiết cho IT để kiểm tra.

**Q19. Làm sao để kiểm tra trạng thái server?**  
A19: Hướng dẫn:

1. Truy cập hệ thống monitoring nội bộ (ví dụ: Zabbix, Grafana).
2. Kiểm tra dashboard trạng thái server, các cảnh báo hoặc lỗi.
3. Nếu phát hiện bất thường, liên hệ IT để xử lý kịp thời.
4. Không tự ý can thiệp vào server nếu không có quyền.

**Q20. Không gửi được email ra ngoài công ty?**  
A20: Kiểm tra các bước:

1. Kiểm tra quota email, xóa bớt email cũ nếu đầy.
2. Kiểm tra chính sách gửi mail, một số domain có thể bị chặn.
3. Kiểm tra lại địa chỉ email người nhận.
4. Nếu vẫn không gửi được, liên hệ IT để kiểm tra hệ thống mail server.

**Q21. Làm sao để đăng ký thiết bị mới vào hệ thống?**  
A21: Quy trình chuẩn:

1. Gửi yêu cầu qua ticket, cung cấp thông tin thiết bị (loại, serial, mục đích sử dụng).
2. IT sẽ kiểm tra và xác nhận thiết bị hợp lệ.
3. Sau khi được phê duyệt, IT sẽ đăng ký thiết bị vào hệ thống quản lý tài sản.
4. Không tự ý kết nối thiết bị lạ vào mạng nội bộ.

**Q22. Không truy cập được tài liệu trên SharePoint?**  
A22: Kiểm tra như sau:

1. Kiểm tra quyền truy cập tài liệu, liên hệ IT để cấp lại quyền nếu cần.
2. Kiểm tra kết nối mạng.
3. Thử truy cập trên trình duyệt khác hoặc xóa cache.
4. Nếu tài liệu bị xóa hoặc di chuyển, liên hệ IT để phục hồi.

**Q23. Làm sao để kiểm tra lịch sử đăng nhập hệ thống?**  
A23: Hướng dẫn:

1. Truy cập trang quản lý tài khoản cá nhân.
2. Xem mục lịch sử đăng nhập, kiểm tra các lần đăng nhập bất thường.
3. Nếu phát hiện truy cập lạ, đổi mật khẩu ngay và báo cho IT.

**Q24. Không nhận được thông báo từ hệ thống?**  
A24: Kiểm tra các bước:

1. Kiểm tra cài đặt thông báo trên hệ thống và email.
2. Kiểm tra bộ lọc email, thư mục spam.
3. Liên hệ IT để kiểm tra hệ thống gửi thông báo.
4. Đảm bảo bạn đã đăng ký nhận thông báo cho các dịch vụ cần thiết.

**Q25. Làm sao để yêu cầu cấp quyền truy cập hệ thống mới?**  
A25: Quy trình chuẩn:

1. Gửi yêu cầu qua ticket, ghi rõ hệ thống cần truy cập và lý do.
2. IT sẽ kiểm tra và phê duyệt dựa trên vai trò công việc.
3. Sau khi được cấp quyền, kiểm tra lại truy cập và báo IT nếu có vấn đề.

**Q26. Không thể cập nhật phần mềm?**  
A26: Xử lý như sau:

1. Kiểm tra kết nối mạng, đảm bảo không bị chặn bởi firewall.
2. Kiểm tra quyền admin trên máy tính.
3. Thử cập nhật lại hoặc cài đặt bản mới nhất từ website chính thức.
4. Nếu vẫn lỗi, liên hệ IT để được hỗ trợ cập nhật.

**Q27. Làm sao để kiểm tra dung lượng ổ cứng?**  
A27: Hướng dẫn:

1. Mở My Computer/This PC, kiểm tra dung lượng từng ổ.
2. Sử dụng phần mềm quản lý ổ cứng nếu cần chi tiết hơn.
3. Nếu ổ cứng đầy, xóa file không cần thiết hoặc liên hệ IT để nâng cấp.

**Q28. Không thể truy cập hệ thống báo cáo tài chính?**  
A28: Kiểm tra như sau:

1. Kiểm tra quyền truy cập hệ thống, liên hệ IT để xác nhận.
2. Kiểm tra kết nối mạng.
3. Thử đăng nhập trên trình duyệt khác hoặc xóa cache.
4. Nếu nhận được thông báo lỗi, gửi chi tiết cho IT để kiểm tra.

**Q29. Làm sao để bảo mật tài khoản cá nhân?**  
A29: Hướng dẫn:

1. Đổi mật khẩu định kỳ, không sử dụng mật khẩu dễ đoán.
2. Không chia sẻ tài khoản với người khác.
3. Bật xác thực 2 lớp nếu hệ thống hỗ trợ.
4. Kiểm tra lịch sử đăng nhập thường xuyên.
5. Báo IT ngay nếu phát hiện truy cập bất thường.

**Q30. Không thể đăng nhập hệ thống do lỗi xác thực?**  
A30: Xử lý như sau:

1. Kiểm tra lại username/password, thử reset mật khẩu nếu quên.
2. Đảm bảo tài khoản chưa bị khóa hoặc hết hạn.
3. Thử đăng nhập trên trình duyệt khác hoặc xóa cache.
4. Nếu vẫn không đăng nhập được, liên hệ IT để kiểm tra và cấp lại quyền.

---

## 🔧 Tài nguyên & Hệ thống nội bộ

### Hệ thống chính:

- **Email:** https://mail.company.com (Outlook Web Access)
- **HR System:** https://hr.company.com
- **ERP:** https://erp.company.com
- **CRM:** https://crm.company.com
- **Project Management:** https://pm.company.com (Jira/Trello)
- **Document Management:** https://docs.company.com (SharePoint)
- **Time Tracking:** https://timesheet.company.com

### Công cụ làm việc:

- **Video Conference:** https://meet.company.com (Zoom/Teams)
- **Chat:** Slack workspace - company.slack.com
- **File Storage:** OneDrive for Business / Company NAS
- **VPN Client:** Cisco AnyConnect / FortiClient
- **Password Manager:** https://vault.company.com

### Thông tin mạng nội bộ:

- **WiFi SSID:** CompanyWiFi-Staff
- **Guest WiFi:** CompanyWiFi-Guest
- **DNS Primary:** 10.0.0.10
- **DNS Secondary:** 10.0.0.11
- **Proxy Server:** proxy.company.com:8080
- **Print Server:** print.company.com

### Tài liệu & Hướng dẫn:

- **IT Knowledge Base:** https://kb.company.com
- **User Manual:** https://manual.company.com
- **IT Policy:** https://policy.company.com
- **Training Videos:** https://training.company.com

### Quy định bảo mật quan trọng:

1. Không chia sẻ mật khẩu với bất kỳ ai
2. Đổi mật khẩu ít nhất 3 tháng/lần
3. Không cài đặt phần mềm không rõ nguồn gốc
4. Khóa máy khi rời khỏi bàn làm việc
5. Không kết nối USB lạ vào máy công ty
6. Báo cáo ngay các email/link đáng ngờ cho IT Security

### Lịch bảo trì định kỳ:

- **Backup hàng ngày:** 23:00 - 01:00
- **Bảo trì hệ thống:** Chủ nhật đầu tiên mỗi tháng, 02:00 - 06:00
- **Cập nhật bảo mật:** Thứ 4 hàng tuần, 19:00 - 21:00

---

## ===========================

## 7. SERVER ACCESS

## ===========================

### Description

| Environment | Access    | Address                            | Notes                       |
| ----------- | --------- | ---------------------------------- | --------------------------- |
| Production  | VPN → SSH | 192.168.80.21                      | No direct DB queries        |
| Staging     | VPN → SSH | 192.168.80.41                      | Read-only DB access allowed |
| Monitoring  | HTTPS     | https://monitor.company-client.com | Admin account in Vault      |

All credentials stored in Vault. Violations lead to account suspension.

---

### Q&A Section

**Q1. How to access the Production server?**  
A1: VPN → SSH to 192.168.80.21. Direct DB queries are blocked.

**Q2. Where are credentials stored?**  
A2: In the Vault system (not in plain text).

**Q3. What happens if access rules are broken?**  
A3: Account suspension and security audit.

---

## ===========================

## 8. VPN TROUBLESHOOTING GUIDE

## ===========================

### Description

**VPN Setup and Troubleshooting Guide**

When users encounter VPN connection issues, follow these steps:

**Step 1: Check Internet Connection**

- Verify the device has a stable Internet connection
- Try accessing public websites (google.com, baidu.com)
- Check network speed: minimum 5 Mbps download, 2 Mbps upload

**Step 2: Check VPN Software**

- Confirm VPN software is installed with the correct version
- Required versions: Cisco AnyConnect 4.10+ or FortiClient 7.0+
- Check VPN service status on the system

**Step 3: Restart VPN Software**

- Completely close the VPN application (not just minimize)
- Wait 10 seconds
- Reopen the VPN application
- Log in again with credentials from Vault

**Step 4: Check VPN Configuration**

- Server address: vpn.company-project.com
- Port: 443 (HTTPS)
- Protocol: SSL/TLS
- Group: Internal-Users (or Development-Team for developers)

**Step 5: Clear Cache and Old Configuration**

- Windows: Delete folder `%ProgramData%\Cisco\Cisco AnyConnect Secure Mobility Client`
- Mac: Delete folder `~/Library/Application Support/Cisco/Cisco AnyConnect`
- Linux: Delete file `~/.anyconnect`

**Step 6: Restart Network Services**

- Windows: Run `ipconfig /flushdns` and `netsh winsock reset` (requires admin)
- Mac: Run `sudo killall -HUP mDNSResponder`
- Linux: Run `sudo systemctl restart NetworkManager`

**Step 7: Check Firewall and Antivirus**

- Temporarily disable Windows Firewall/Defender
- Temporarily disable Antivirus software
- Try connecting to VPN again
- If successful, add exception for VPN client

**Step 8: Check SSL Certificate**

- Ensure system date/time is accurate (more than 5 minutes off will cause SSL errors)
- Verify VPN server certificate is still valid
- Update root certificates if necessary

**Step 9: Contact IT Support**

- If the above steps do not resolve the issue, create a ticket in Jira
- Provide the following information:
  - Error code (if available)
  - Error screenshot
  - Log file from VPN client
  - Time when the issue occurred

**Support Information:**

- IT Helpdesk: support@company-project.com
- Hotline: +84-xxx-xxx-xxxx (business hours)
- Ticket System: https://jira.company-project.com

---

### Q&A Section

**Q1. How to fix VPN connection errors?**  
A1: Follow these steps: 1. Check Internet connection, 2. Restart VPN software, 3. Check VPN configuration (server: vpn.company-project.com, port: 443), 4. Clear VPN cache, 5. Restart network services, 6. Check Firewall/Antivirus, 7. Check SSL certificate, 8. Contact IT Support if still not working.

**Q2. What are the VPN connection details?**  
A2: Server address: vpn.company-project.com, Port: 443 (HTTPS), Protocol: SSL/TLS, Group: Internal-Users or Development-Team.

**Q3. What should I do if I still cannot connect after trying these steps?**  
A3: Create a ticket in Jira with error code, screenshot, log file, and time when the issue occurred. Contact IT Helpdesk: support@company-project.com.

**Q4. Which VPN client versions are supported?**  
A4: Cisco AnyConnect 4.10+ or FortiClient 7.0+.

**Q5. Why does VPN require accurate system date and time?**  
A5: VPN uses SSL/TLS certificates. If system date/time is off by more than 5 minutes, it will cause certificate authentication errors.

---

## ===========================

## 9. SYSTEM ARCHITECTURE & ROUTING

## ===========================

### Description

**System Architecture and Routing Structure**

The system uses a microservices architecture with API Gateway as the single entry point. Services are deployed on a Kubernetes cluster.

**Architecture Overview:**

- **API Gateway**: Entry point for all requests from clients
- **Service Registry**: Manages service discovery (Consul)
- **Load Balancer**: Distributes traffic across service instances
- **Database Cluster**: PostgreSQL with replication and failover

**Service X - Routing Structure:**

Service X is a microservice that handles core business logic. Routing is configured through Kubernetes Ingress and internal service mesh.

**Ingress Configuration (YAML):**

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: service-x-ingress
  namespace: production
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /$1
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - api.company-project.com
      secretName: service-x-tls
  rules:
    - host: api.company-project.com
      http:
        paths:
          - path: /api/v1/service-x(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: service-x
                port:
                  number: 8080
          - path: /api/v1/service-x/health
            pathType: Exact
            backend:
              service:
                name: service-x-health
                port:
                  number: 9090
```

**Service Configuration (YAML):**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: service-x
  namespace: production
  labels:
    app: service-x
    version: v1
spec:
  type: ClusterIP
  selector:
    app: service-x
    tier: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
```

**Deployment Configuration (YAML):**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-x
  namespace: production
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: service-x
      tier: backend
  template:
    metadata:
      labels:
        app: service-x
        tier: backend
    spec:
      containers:
        - name: service-x
          image: registry.company-project.com/service-x:v1.2.3
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 9090
              name: metrics
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: service-x-secrets
                  key: db-host
            - name: DB_PORT
              value: "5432"
            - name: LOG_LEVEL
              value: "info"
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 9090
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 9090
            initialDelaySeconds: 5
            periodSeconds: 5
```

**Internal Routing Rules:**

Service X uses internal routing based on path and method:

- `GET /api/v1/service-x/users` → User Management Service
- `POST /api/v1/service-x/orders` → Order Processing Service
- `GET /api/v1/service-x/reports` → Reporting Service
- `PUT /api/v1/service-x/config` → Configuration Service

**Load Balancing Strategy:**

- Round-robin for regular requests
- Session affinity (sticky session) for stateful requests
- Health check every 5 seconds to remove unhealthy instances

**Network Policy (YAML):**

```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: service-x-network-policy
  namespace: production
spec:
  podSelector:
    matchLabels:
      app: service-x
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: production
        - podSelector:
            matchLabels:
              app: api-gateway
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: postgres
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - podSelector:
            matchLabels:
              app: redis
      ports:
        - protocol: TCP
          port: 6379
```

---

### Q&A Section

**Q1. What is the routing structure of Service X?**  
A1: Service X uses Kubernetes Ingress with path `/api/v1/service-x(/|$)(.*)` routing to the service on port 8080. Health check endpoint is at `/api/v1/service-x/health` on port 9090. Internal routing is based on path: `/users`, `/orders`, `/reports`, `/config`.

**Q2. How does Service X use YAML configuration?**  
A2: Service X is configured through 4 types of YAML: Ingress (routing from Internet), Service (internal service discovery), Deployment (container orchestration), and NetworkPolicy (security rules). All are deployed in the `production` namespace.

**Q3. How does Service X handle load balancing?**  
A3: Service X uses round-robin for regular requests and session affinity (sticky session) for stateful requests. Health check every 5 seconds to remove unhealthy instances. Deployment has 3 replicas with rolling update strategy.

**Q4. Which ports are used for Service X?**  
A4: Port 8080 for HTTP traffic, port 9090 for metrics and health check.

**Q5. What connections does Service X network policy allow?**  
A5: Ingress only allows from the `production` namespace and from `api-gateway` pod. Egress only allows connections to PostgreSQL (port 5432) and Redis (port 6379).

**Q6. How many replicas does Service X have?**  
A6: Service X has 3 replicas with rolling update strategy (maxSurge: 1, maxUnavailable: 0).

---

_Last Update: 2025-11-08_  
_Author: IT Technical Support Team_  
_Version: 2.0_
